#include <iostream>
#include <string>
#include <stack>

using namespace std;

int main (){

	stack<char> numeros;
	string str;
	cin >> str;
	int i = 0;
	while(str[i] == '1' || str[i] == '0'){
		numeros.push(str[i]);
		i++;
	}

	int tam = numeros.size()
	for (int i  = 0; i < tam; i++){
		
	}

	return 0;
}